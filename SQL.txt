SELECT * FROM movies
ORDER BY year DESC;

INSERT INTO directors(first,last,country)
VALUES(Jean-Pierre, Jeunet, France)

SELECT * FROM Directors
ORDER BY first = "Jean-Pierre and last = "Jeunet" and director_id

INSERT INTO movies(year,director_id,title)
VALUES(2001,2,'Amelia')


SELECT * FROM directors
ORDER BY country ASC;

SELECT movies.title, directors.country
FROM directors
INNER JOIN movies ON directors.country = movies.title


SELECT movies.title, directors.first, directors.last
FROM movies
INNER JOIN directors ON movies.title
ORDER BY directors.last ASC